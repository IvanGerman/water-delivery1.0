import React, { FC } from 'react';

import styles from './Menu.module.scss';

const Menu: FC = () => {
  return (
    <div className={styles.menuWrapper}>
      Menu
    </div>
  );
};

export default Menu;
